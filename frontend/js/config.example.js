/**
 * Ya no es obligatorio copiar este archivo: config.js en el repo detecta el entorno.
 *
 * Solo si necesita forzar una URL (p. ej. API en otra IP de la LAN), edite config.js
 * o defina antes de cargar scripts:
 *   window.__RIS_API_URL__ = 'http://192.168.1.50:8000/api';
 */
const API_URL = 'https://api.healthticloud.cl/api';
